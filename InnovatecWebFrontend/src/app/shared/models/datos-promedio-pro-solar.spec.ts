import { DatosPromedioProSolar } from './datos-promedio-pro-solar';

describe('DatosPromedioProSolar', () => {
  it('should create an instance', () => {
    expect(new DatosPromedioProSolar()).toBeTruthy();
  });
});
